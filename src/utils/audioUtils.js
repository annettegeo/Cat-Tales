export const getWordTimestamps = () => {
    // Return timestamps for each word in the audio
    // Adjust these values based on your actual audio file
    return [
      { start: 0, end: 0.5 },
      { start: 0.6, end: 1.1 },
      { start: 1.2, end: 1.7 },
      { start: 1.8, end: 2.3 },
      { start: 2.4, end: 2.9 }
    ];
  };